migration.up = function(db) {
// add a Autoincrement in a migration 
//INTEGER PRIMARY KEY AUTOINCREMENT
//migrator.db.execute('ALTER TABLE ' + migrator.table + ' ADD COLUMN index INTEGER PRIMARY KEY AUTOINCREMENT ;');

};

migration.down = function(db) {

};
